const toggleMenu = () => {
    $('.toggle-mobile-menu').toggleClass('open');
    $('.mobile-menu').toggleClass('visible');
}
const toggleSearch = () => {
    $('.search').toggleClass('open');
}
const toggleCart = () => {
    $('.bg-effect').toggleClass('active');
    $('.cart').toggleClass('open');
}